# name : CamerPay - CI/CD Pipeline

# on: 
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

# jobs:
#     validation:
#         name: Validation du code
#         runs-on: ubuntu-latest
#         steps:
#             - name: recuperer le code
#               uses: actions/checkout@v4
#             - name: verifier l'absence de console.log          
#             run: |
#                 if grep -rm 'console.log' --include='*.js' .;then
#                     echo 'Erreur: Des console.log ont été trouvés dans le code!!'
#                     exit 1
#                 fi
#                 echo 'OK: Aucun console.log trouvé dans le code.'

#     deploy:
#         name: Deploiement de l'application                  